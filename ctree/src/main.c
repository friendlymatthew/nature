
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
Ctree is a visualization program that uses OpenGL's 3D engine to draw trees.

- uses the space colonization algorithm
- optimized using quadtree data structure
*/
int main(void) {
    // init random seed
    srand(time(NULL));


    printf("howdy world");

    return 0;
}
